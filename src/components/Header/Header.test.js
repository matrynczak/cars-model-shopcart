import renderer from 'react-test-renderer';
import Header from './Header';

describe('Header renders correctly', () => {
  test('Without any items in cart', () => {
    // const quantity1 = 1;
    // const tree = renderer
    // .create(<Header quantity={quantity1} />)
    //   .toJSON();
    // expect(tree).toMatchSnapshot();
  });
})

